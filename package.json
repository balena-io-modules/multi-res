{
  "name": "multi-res",
  "version": "1.0.0",
  "description": "An express middleware to respond to multipart requests",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "npm run lint && coffee -c lib",
    "lint": "resin-lint lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Giovanni Garufi",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.2",
    "mimemessage": "git://github.com/resin-io-modules/mimemessage.js"
  },
  "devDependencies": {
    "versionist": "^2.8.0"
  }
}
